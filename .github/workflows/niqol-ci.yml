name: niqol CI

on:
  pull_request:
    paths:
      - "Cargo.tml"
      - "Cargo.lock"
      - "flake.nix"
      - ".github/workflows/niqol-ci.yml"
  push:
    branches: [ main ]
    paths:
      - "Cargo.tml"
      - "Cargo.lock"
      - "flake.nix"
      - ".github/workflows/niqol-ci.yml"
  workflow_dispatch:

jobs:
  nix-build-package:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Build niqol Nix package
        run: nix build

  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Check formatting
        run : nix develop -c cargo fmt --check

      - name: Clippy
        run: nix develop -c cargo clippy --all-targets --all-features -- -D warnings

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Check formatting
        run: nix develop -c cargo test
